# 實驗數據產出規格 (PC 端)

## 背景與定位

本 repo 已有一份完成度高的 Viola-Jones 人臉偵測 C 實作（25 stages / 2913 features，
權重來自 `haarcascade_frontalface_default.xml`）。演算法正確性與 OpenCV 行為對齊的工作
**已完成**，不需重做核心偵測邏輯。

這個任務的目的，是為一場研究會議產出 **PC 端的量化實驗數據**，用來支持一個更大的研究主題：
把 Viola-Jones 作為 rPPG 系統晶片的 ROI 偵測前處理。會議要回答的核心問題是
**「這個 C 實作相對於主流偵測器，在準確度 vs 運算成本上落在哪裡」**。

**重要邊界（請勿越界）：**
- 不要改動 `src/` 下的核心偵測演算法邏輯。如發現 bug 需修正，先標註再修，並在報告中說明。
- 不要做「無乘法 / 定點化 / 硬體友善版」改寫 — 那是會議後的下一階段，不在本次範圍。
- 不要碰 STM32N6 或任何嵌入式部署 — 本次純 PC 端。
- 所有新增程式碼放在 `tools/` 或新建 `experiments/` 目錄，與既有 code 隔離。

## 既有可複用的基礎設施（先讀過再動工）

- `tools/verify.py` — 已能跑 C detector 與 OpenCV 並比對，但目前只比「偵測數量」，
  缺真正的量化指標。請在此基礎上擴充，不要重寫。
- `tools/visualize.py` — 已能並排畫框（OpenCV vs C）。
- `tools/convert_cascade.py` — xml → C 陣列。本次用不到，但了解資料來源有幫助。
- `build.bat` — Windows/MinGW build。請確認可在目標環境 build；若在 Linux/Mac，
  需自行寫對應 build（gcc，flags 參考 build.bat）。
- C detector 介面：`test/test_main.c`，吃 raw 8-bit grayscale，輸出偵測框文字。

## 要交付的實驗（三組，按優先序）

### 實驗一：C 實作 vs OpenCV 一致性（最高優先，保底必交）
證明這份 C 實作的偵測結果與 OpenCV 官方實作「實質等價」，這是後續所有比較的信任基礎。
- 不能只比偵測數量。要算 **IoU**，並以 IoU 門檻（如 0.5）判定 true/false positive。
- 在多張含人臉影像上，報告 C vs OpenCV 的：偵測框 IoU 分布、框數量差異、座標偏差。
- 預期結論：兩者高度一致；若有系統性偏差，量化它並給出可能原因。

### 實驗二：準確度比較 vs 現代偵測器（核心，會議重點）
與一個現代 CNN-based 偵測器比較（YOLO-face、或 OpenCV DNN face detector、
或 MediaPipe/BlazeFace 任一個容易取得的即可，自行選最易整合者）。
- **關鍵：比較的框架必須公平且切題。** 直接比準確度，CNN 一定贏，這不是重點也對我方不利。
  要呈現的是 **「準確度 vs 運算成本」的 trade-off**，不是單純準確度排名。
- 指標：在同一組測試影像上，報告各偵測器的 precision / recall（以 IoU 判定），
  並**並列**各自的運算成本（見實驗三）。讓讀者看到「Viola-Jones 用極低成本換取了可接受的準確度」。
- 測試資料：用 repo 內既有影像起步即可；若能輕量取得一個小型公開人臉資料集
  （如 WIDER FACE 的子集、或任何幾十張帶標註的臉）會更有說服力，但別讓資料下載
  變成阻塞點 — 取不到就用手邊影像並說明樣本限制。

### 實驗三：Latency 量測（核心，會議重點）
量測並比較運算成本。這是支撐「Viola-Jones 適合做低成本硬體加速器」論點的數據。
- 量 C 實作的 per-image latency（同一組影像、多次取平均、報告 mean/std）。
- 同條件下量現代偵測器的 latency 作對照。
- 若能額外提供更接近「運算量本質」的指標會加分（例如：每張圖的視窗評估次數、
  cascade 各 stage 的 early-rejection 比例 — 後者能直觀展示 cascade 為何快）。
- 注意公平性：同一台機器、同一組影像、講清楚是否含 I/O、是否暖機。

## 產出格式

1. 一份 `experiments/RESULTS.md`：用表格呈現三組實驗結果，每組附 2-3 句解讀。
   會議要直接看這份，所以要清晰、數字到位、結論明確。
2. 可重現的腳本放在 `experiments/`，附一個 `README` 說明如何重跑。
3. 視覺化輸出（並排偵測框、trade-off 散點圖：x=latency、y=準確度）存成圖檔。
4. 若過程中發現任何 C 實作的潛在問題（如浮點精度、grouping 邊界），記在 RESULTS.md
   的「Notes / 已知限制」段落，不要默默吞掉。

## 驗收標準

- [ ] 實驗一有 IoU-based 的量化一致性結論，不只是數量比對。
- [ ] 實驗二的比較是「準確度 vs 成本 trade-off」框架，不是單純準確度排名。
- [ ] 實驗三的 latency 是公平條件下量的，講清楚量測方法。
- [ ] RESULTS.md 可讓人在不看 code 的情況下看懂結論。
- [ ] 所有腳本可重跑，未改動 `src/` 核心邏輯（或改動有明確標註與理由）。

## 給實作者的提醒

優先序是 實驗一 > 實驗三 > 實驗二（一致性是地基、latency 是我方強項、準確度比較最花工
且要小心框架）。時間有限就先確保前兩個紮實。遇到設計抉擇時，以「會議能不能用這個數字
講出清楚的故事」為判準。
